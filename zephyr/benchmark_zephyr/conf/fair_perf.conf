# SPDX-License-Identifier: GPL-3.0-or-later
#
# fair_perf profile (ADR-011): kernel-pure measurement.
# Tickless OFF, no WFI in idle.

CONFIG_BENCH_PROFILE_FAIR_PERF=y

CONFIG_TICKLESS_KERNEL=n
CONFIG_PM=n
