# Build artifacts
build/
build_*/

# E1a deliverable archive output (generated, never committed)
dist/
*.o
*.a
*.elf
*.bin
*.hex
*.map
*.lst
*.list
*.dmp
*.lss
*.su
*.d

# ChibiOS specific
chibios/benchmark_chibios/build/

# FreeRTOS / CMake
freertos/benchmark_freertos/build/
freertos/benchmark_freertos/cmake-build-*/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile.cmake

# Zephyr / west
zephyr/build/
zephyr/.west/

# Host unit-test binary
tests/host/test_bench_stats
tests/host/test_bench_stats.exe
zephyr/modules/
zephyr/zephyr/
zephyr/bootloader/
zephyr/tools/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# IDE
.vscode/
.idea/
*.code-workspace
.cproject
.project
.settings/
.launch
*.launch
.eclipse-workspace/
.metadata/
launchers/

# OS
.DS_Store
Thumbs.db
*.swp
*~

# Results — not published; only the synthesis under docs/ is committed
results/

# Local Claude Code instructions (personal, never committed)
CLAUDE.local.md

# Persistent notes folder (personal context memory, never committed)
notes/

# Local toolchain: ~3 GB of binaries never committed; only the
# small checksum lock IS committed (ADR-021).
tools/*
!tools/TOOLCHAIN.lock

# Reference material (third-party docs, internal review notes — never committed)
reference/

# AI tooling (personal, never committed)
.claude/
.codex/
AGENTS.override.md
AGENTS.md

# Stray build / capture logs (must never land in the source tree)
*_build_log.txt
**/lab_campaign_*.log
